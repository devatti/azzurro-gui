services:
  web:
    build: .
    container_name: azzurro-dashboard
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DJANGO_DB_PATH: /data/db.sqlite3
    volumes:
      - sqlite_data:/data

volumes:
  sqlite_data: